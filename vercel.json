{
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "installCommand": "npm install",
  "devCommand": "nodemon index.js",
  "builds": [
    {
      "src": "index.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      "src": "routes/(.*)",
      "dest": "index.js"
    }
  ]
}
